optConfig:
  optOutStrategy: true
  optOutRepos:
  # rekor uses binary artifacts in tests/
  - rekor
  # sigstore-maven uses binary artifacts in .mvn and in src/test/
  - sigstore-maven
  # timestamp-authority uses binary artifacts in hack/tools/
  - timestamp-authority

action: issue
