optConfig:
  optOutStrategy: true
  optOutRepos:
    # rekor uses binary artifacts in tests/
    - rekor
    # sigstore-maven uses binary artifacts in .mvn and in src/test/
    - sigstore-maven
    # protobuf-specs uses binary artifacts in java/gradle/wrapper
    - protobuf-specs

action: issue
